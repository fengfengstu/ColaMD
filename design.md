# ColaMD Design

ColaMD 的界面设计规范。所有功能、视觉和交互改动都应以本文档为基准；如果新需求与规范冲突，应先更新规范，再实现功能。

> Writing should be this simple.

## 1. 设计原则

### 如非必要，勿增实体

ColaMD 是轻量 Markdown 编辑器，不追求功能堆叠。每增加一个按钮、图标、提示或面板，都必须回答：

1. 它解决了什么明确问题？
2. 是否可以用已有的 Markdown 语法、快捷键或菜单完成？
3. 它是否会增加持续的视觉噪音？

默认不增加。必要时优先使用已有控件和已有交互模式。

### 内容优先

- 编辑器内容是界面中心，工具控件不得抢夺注意力。
- 控件默认低调，只有 hover、focus 或 active 时才提供明显反馈。
- 不使用常驻工具栏和状态栏。
- 不用装饰性 UI 模拟功能。

### 一致性优先

同一类问题只允许一种主要解决方式。同类控件必须共享尺寸、间距、颜色、圆角、动画和图标语言；不要为单个场景发明例外。

## 2. 页面结构

### 窗口

- 标题栏高度：`40px`。
- 标题栏用于拖拽，不承载多余信息。
- 右上角只放必要的轻量操作：文档统计、源码模式、文件面板。按钮 `28px × 28px`，`top: 6px`，间距 `36px`，最右按钮 `right: 6px`。不要在这里放新建标签页的加号：它会在标签栏出现时突然消失，这个观感被否掉。
- 标题栏不再承载文件名，也没有「打开所在文件夹」按钮（2026-09-15）：文件名由标签承担，在文件管理器中显示改由标签右键菜单承担。
- **标题永远居中**：标题栏中部的辅助元素（悬停出现的按钮、保存状态）一律绝对定位，不参与标题的居中计算。标题只能因文件名本身改变宽度，出现「已编辑」不许把标题挤偏。
- 控件需要设置 `-webkit-app-region: no-drag`，避免影响窗口拖拽。
- **窗口尺寸与视图缩放要被记住**（2026-09-15 定）：每次启动都要重新调窗口大小和缩放，是 2K 屏幕用户每天都在碰的麻烦。保存窗口的位置与尺寸（`getNormalBounds`，全屏或最大化时不会把屏幕尺寸当成窗口尺寸），以及视图缩放倍数；下次启动按保存值开窗并恢复缩放。保存的位置如果不在当前任何一块屏幕上（比如外接屏拔了），就退回默认尺寸，不能把窗口开到看不见的地方。

### 编辑器

- 编辑区域最大内容宽度：`780px`。
- 内容水平居中。
- 默认左右内边距：`40px`。
- 正文字号：`16px`。
- 正文行高：约 `1.75`。
- 编辑器应始终保持足够的留白，不为了填满窗口而扩大内容宽度。
- **「已编辑」必须有基线**：编辑器初始化、程序化载入内容（打开文件、恢复会话、载入示例）都不算用户编辑，文档必须保持干净；只有用户真正改动内容才显示「已编辑」并触发保存提示。新建窗口必须从干净状态开始，未动过一个字就关窗不该问要不要保存。

### 标签

ColaMD 是一块沉浸式的画布，外壳要克制：标签栏和标题栏都不使用主题色，选中状态由纸张色与底边开口表达，不额外加装饰线。

标签栏的左右留白不对称：左边 6px（第一个标签贴着窗口左边缘，14px 会显得空出一块），右边 14px（给标签栏右端的加号留位置）。

- 标签栏**始终存在**（2026-09-15 定）：一个文档也是一个标签，顶部永远同一个形状，不会因为开了第二个文档而变一副样子。代价是原来居中显示的文件名不再出现，文件名由标签承担。
- **新建标签页的入口**（2026-09-13 定）：`⌘T`、File 菜单的「新建标签页」、文件列表里 ⌘ 加点击、文件列表右键的「在新标签页打开」（排在该菜单第一位），以及**标签栏右端紧跟着最后一个标签的那个加号**。标题栏不放加号：它会在标签栏出现时突然消失，这个观感被否掉；两个加号同时存在更不行。
- 标签栏**与标题栏共用同一行，且不是浮层**（2026-09-15 定）：一个窗口顶部永远只有一行，像浏览器那样。标签条是标题栏的第一个弹性子元素，不是 `position: absolute` 的第二层（`58f66f4` 曾做成 z-index 31 的浮层压在标题栏上，线、色、按钮三层症状全部源于这个分裂）。顶行只有一个背景（标题栏的 `--chrome-bg`）、一条基线（标题栏自己的 `inset box-shadow`，活动标签以不透明纸张色贴住行底把它切开，开口通向正文）、一套层级。标签条从红绿灯右侧开始（标题栏 `padding-left: 84px`），右侧按钮组与「已编辑」是同一行的弹性成员，由布局对齐，不用 `right: 78/42/6/108` 魔法数字互相对齐。标签栏固定只有一行，超出时横向滚动，不换行。
- 共用一行时**不再单独显示文件名**（文件名由标签承担，活动标签就是当前文档），悬停文件名才出现的「打开所在文件夹」也随之不再出现，这个能力由标签右键菜单的「在文件管理器中显示」承担。右上角的三个按钮（文档统计、源码模式、文件面板）位置不变；「已编辑」仍然要看得见，锚定在右侧按钮组左边。空的那一段仍然可以拖拽窗口。
- 标签条从红绿灯右侧（`left: 84px`）开始，横跨整窗，不缩进到面板右边缘：试过按面板宽度缩进，看起来像多了一道空带，已否掉。
- 标签栏的视觉（2026-09-13 定）：选中的标签回到「纸张色 + 三边细边、底边开口」的画法，与正文连成一体；方块角、无阴影，不加强调色装饰线（曾把一根 `1px` 强调色细线放在选中标签上边，2026-09-13 随 v2.1.2 去掉——纸张色对底色的对比本身就是选中态，加线让标签栏抢夺注意力）。试过并否掉的方案：X 那种整个顶部一整块、选中项底下一条指示线、`1px` 强调色细线，以及 2px 的粗犷强调线。方块角是全界面唯一的圆角例外，是有意保留的：选中标签要和下方正文连成一张纸，圆角会在底边开口处留下缺口。
- **打开文件的落点**（2026-09-15 定）：已经打开的文档 → 聚焦它所在的窗口并激活对应标签页，不重复打开；否则当前标签页空白且未修改 → 载入当前标签页（复用）；否则在**当前窗口新开一个标签页**。多窗口是用户主动要的（`⌘N`），不是打开文件的副作用。这条覆盖 `⌘O`、拖拽、最近文件、系统「用默认应用打开」，以及一次打开多个文件（第一个开窗口，其余作为标签进入同一个窗口）。
- **文件列表的点击是浏览，不是打开**：仍然载入当前标签页（换目录看文件不应该越点标签越多，这条沿用旧行为）。
- 标签页显示文件名，过长时省略号截断并用悬停提示给出全名；未保存状态在该标签上用一个弱化的小圆点表示（标题栏的「已编辑」仍是主要提示，不另做一套文字）。
- 标签宽度跟着文件名走：贴着文件名，不留多余空白，文件名在标签里左右居中（内边距对称）；最小宽度 `50px`（与实现对齐：`28px` 文字 + `20px` 内边距 + `2px` 边框，正好放下「a.md」这类极短文件名，不再退化成一个裸正方形）；最长 `190px` 后截断。
- 标签页交互：点击切换；中键或 `⌘W` 关闭；右键菜单为「关闭 / 关闭其他 / 关闭右侧 / 在新窗口打开 / 复制路径 / 在文件管理器中显示」，用系统原生菜单。「在新窗口打开」只在标签有路径时出现（未命名标签没有可打开的文件）。
- 标签右键菜单：没有路径的未命名标签只给关闭相关的项；「关闭右侧」只在右边真有标签时出现。「关闭其他 / 关闭右侧」逐个别关，每一项都可能弹未保存确认，用户取消就停下。
- 标签悬停提示：停够 **1 秒**才出现（悬停通常是为了点它，立刻弹字很吵）；名字没被截断时只显示 `⌘W`，被截断时才显示「全名 · ⌘W」。
- **不要给标签加 `title` 属性**：那是系统浮层，停一会儿就会冒出完整路径，既慢又不像我们自己的东西。要提示就用 `#tab-tip` 这一层。
- 滚动条交给系统：macOS 用系统自带的浮层滚动条（滚动时出现、不占版面），只有 Windows / Linux 才用细的自定义滚动条。Chromium 不会因 hover 重新绘制自定义滚动条（实测过：`*:hover::-webkit-scrollbar-thumb` 与逐容器写法都不上色），所以「鼠标悬停才显示滑块」做不了，不要反复尝试。
- 正文区下方的空白必须“能点”：最后一行以下的空处也要把光标放进去。做法是让 Milkdown 的包裹层 `min-height: 100%`（相对编辑器自己的高度算，不写 `100vh` 减法），内部的 `.ProseMirror` 再 `flex: 1 0 auto` 铺满。2026-09-13 曾把 `display: flex` 加在 `#editor` 上，当时真正的编辑区（`.ProseMirror`）反而没长高，导致空白处点不出光标。
- 切换标签页不得丢掉改了还没存的内容：有路径的标签先保存再切走；未命名标签的内容留在它自己的快照里，只在真正会被丢掉时（关标签、关窗口）才问用户。
- 只有活动标签监听文件变化，不为每个标签各养一个监听器。切回一个标签时校验它的磁盘版本：没有未保存改动就采用磁盘版本，有未保存改动则走「文件已被外部修改」流程。
- `⌘W` 关闭当前标签页，关掉最后一个标签页时关窗口（macOS 惯例）。未保存的标签页关闭前同样要确认。
- 每个标签独立持有：内容、未保存状态、撤销历史、滚动位置、源码 / 所见即所得模式。切换标签页不许互相影响。
  - 实现方式：只用一个编辑器实例，每个标签保存一份编辑器状态快照（撤销栈就在这份状态里），切换就是换状态，既快又能保证撤销不串标签。
- **标签页不是工作区**：不持久化，重启不恢复标签组；不做固定标签、标签分组、跨窗口拖拽标签、全量文件树。
- 标签页与窗口共存：`⌘N`（文件 → 新建窗口）仍然是独立窗口，标签页不跨窗口移动。

### 文件列表面板

- 默认宽度：`220px`；允许用户拖拽调宽，范围 `180px`–`420px`，并记住用户的选择。
- 拖拽把手必须轻量：不显示常驻把手图标，仅边缘热区 + hover 反馈。
- 仅使用轻量文件列表，不做持久化工作区和全量文件树（标签页同样不持久化，见上）。
- 打开文件时浏览当前文件所在目录。
- 没有打开文件时，默认浏览系统「文稿」目录；不可用时回退到桌面。
- 支持进入目录和返回上级目录。
- 「文件 / 大纲」切换保持平面极简（2026-09-15 定）：托盘用 `--chrome-bg`，活动片用纸色 `--bg-color`，无阴影、无浮起、无立体感；阴影和浮起与全软件的平面风格不搭，已否掉。
- 面板可通过 `⌘⇧B` 隐藏或显示，并记住用户的选择。
- 文件条目支持右键菜单，用系统原生弹出菜单，不自绘菜单、不新增常驻按钮。文件的第一项固定是「在新标签页打开」，下面一条分隔线，之后是「复制路径 / 用默认应用打开 / 在文件管理器中显示」；目录为「复制路径 / 在文件管理器中显示」（目录没有「在新标签页打开」）；「上级目录」不提供菜单。

## 3. 图标规范

### 图标语言

- 功能图标统一使用线性 SVG。
- 禁止使用 Unicode 字符、Emoji 或系统字体字符替代功能图标，例如 `↰`、`▸`、`▶` 等。
- 同一组图标使用统一的 `viewBox`，默认 `0 0 16 16`。
- 默认尺寸：`16px`；文件列表中的图标可使用 `14px`。
- 默认线条：`stroke-width: 1.3`。
- 默认端点和连接：`stroke-linecap="round"`、`stroke-linejoin="round"`。
- 图标不得使用填充块制造与其他图标不一致的视觉重量，除非该图标本身表达选中或状态。

### 图标控件

每个图标按钮必须同时具备：

- 可识别的图形；
- `title`；
- `aria-label`；
- hover 时显示的简短文字说明；
- active、disabled 和 hover 状态（如果该控件存在这些状态）。

文字说明使用自然、简洁的中文，例如「显示 / 隐藏文件列表」，不要使用生硬的直译或只写英文 API 名称。

### 文件列表图标

文件、文件夹、返回上级必须使用同一套线性图标：

- 文件：文档轮廓；
- 文件夹：文件夹轮廓；
- 返回上级：线性方向箭头；
- 三者线宽、颜色、尺寸和垂直对齐必须一致。

图标样式放在共享 CSS 中，业务逻辑只负责区分类型和填充名称。

## 4. 颜色与层次

使用语义化 CSS 变量，不在组件中随意写主题专属颜色：

| 语义 | 变量 |
| --- | --- |
| 背景 | `--bg-color` |
| 正文 | `--text-color` |
| 次要文字 | `--text-secondary` |
| 弱化图标 / 禁用状态 | `--text-muted` |
| 边框 | `--border-color` |
| 链接 / 选中 | `--link-color` |
| 代码背景 | `--code-bg` / `--code-block-bg` |
| 搜索匹配 | `--search-match-bg` / `--search-match-current-bg` |

约定：

- 默认控件背景为透明。
- hover 使用低透明度的中性色背景，不制造新的色块。
- active 状态可以使用主题强调色，但必须克制。
- 次要文字使用 `--text-secondary`，与主题背景至少保持 WCAG AA 对比度。
- 次要图标使用 `--text-muted`，不要借给需要阅读的文字。
- tooltip 使用主题背景、边框和轻微阴影，不使用高饱和气泡。

## 5. 尺寸与动效

- 小型按钮默认：`28px × 28px`。
- 小型控件圆角：`6px`。
- 文件列表行间距保持紧凑，避免面板过度膨胀。
- hover、active 的颜色变化使用约 `0.15s` 过渡。
- tooltip 使用轻微淡入和向下位移动画，不使用弹跳或夸张缩放。
- 动效只用于表达状态，不用于装饰。

## 6. 交互规范

- 重要操作必须可以通过菜单或快捷键完成，不能只依赖隐藏图标。
- hover 说明不能遮挡主要内容，也不能阻止鼠标操作。
- disabled 控件必须降低透明度并停止 hover 强调。
- **标题栏是窗口拖拽区（`-webkit-app-region: drag`），其中的元素不会派发鼠标事件**：点击、mouseenter、hover 都不会触发。控件必须自己标 `-webkit-app-region: no-drag` 才能被点击。
- **删 CSS 规则必须连规则体一起删，禁止把选择器并进下一条规则**（2026-09-15 教训）：`58f66f4` 删 `#reveal-file-btn:hover` 时，把冒号选择器组直接接到了下一条 `.toolbar-tip` 定位规则上，等于给三个标题栏按钮写了「hover 时移位并隐藏」，表现为图标闪烁、无法点击。这类错误 tsc 查不出来，只有真窗口里逐个 hover 标题栏控件才暴露。
- **界面表面的颜色一律从主题变量派生，禁止写死**（2026-09-15 定）：hover、容器、托盘的灰面用 `color-mix(in srgb, var(--text-color) N%, transparent)` 或 `--chrome-bg / --chrome-bg-hover`；文件列表与大纲的选中项用中性抬升面（`--chrome-bg-hover` + 正文色文字），不用强调色——选中是「位置」不是「重要」，彩色只属于正文里的链接，不进外壳。纯黑的投影和遮罩可以保留（黑白在深浅主题里语义相同）。写死的 `rgba(127,127,127,…)` 在深色主题里不可见、写死的橙色在深色主题里刺眼，都会破功。
- **主题派生变量必须定义在 `body` 上，禁止放在 `:root`**（2026-09-15 教训）：主题用 `body.theme-*` 覆盖变量，html 永远看不到这些覆盖；放在 `:root` 的派生变量会在 html 层用浅色默认值算死，标题栏从此不跟主题。事故现场：`--chrome-bg` 曾放在 `:root`，深色主题下标题栏保持浅灰、右上角三个按钮融进背景。
- **每次改 UI，深色和浅色主题各过一遍**（2026-09-15 定）：只看浅色的 UI 审查等于没审完。
- **标题栏内可以依赖 hover 显隐控件，但触发点必须自身可接收指针事件**：把 hover 触发点放在文件名（`no-drag` + `pointer-events: auto`）上，标题栏内的 hover 显隐是可靠的。`pointer-events` 会继承，「打开所在文件夹」按钮曾经因为父容器 `#title-center` 是 `pointer-events: none` 而跟着继承 `none`（该按钮 2026-09-15 随标签栏常驻一起移除，教训保留）：悬停文件名时能看到它，指针一移上去它就消失且再也碰不到，表现为「hover 被改丢了」。控件必须自己声明 `pointer-events: auto`（仅 `disabled` 时用 `none`）。
- **hover 显隐控件的触发范围要覆盖整条移动路径**：触发点（文件名）与控件之间有空隙时，指针穿过空隙会让控件闪一下。用控件的 `::before` 铺一块不可见命中区跨过空隙；同时触发范围不得扩到无关控件（右上角那几个按钮 hover 时不许把「打开所在文件夹」带出来）。
- **盖住文字的按钮不得半透明**：叉号、关闭按钮这类「浮在文字上的控件」，出现时必须 100% 不透明，否则文字会从按钮里透出来。用元素级 `opacity` 做「弱化」会让背景一起变半透明（曾用 `opacity: 0.75`，背景也只剩 75%），要弱化就弱化字形颜色（`color`），不要动 `opacity`。
- **遮罩色必须等于它底下的表面色**：上条控件的背景要跟它所在表面完全同色（活动标签用 `--bg-color`，其余用 `--chrome-bg-hover`），并且要等这层表面淡入结束再出现（`transition: opacity 0s 0.15s`），否则不透明遮罩会盖在半透明表面上成为一个色块。
- **禁用态不要当作显隐门**：需要 hover 才出现的控件，不要再把它同时设成 `disabled` 又用 `:not(:disabled)` 显隐，否则一处遗漏就会表现为「hover 失灵」，排查成本高。
- **悬浮提示不得淡入**：提示本身不加 `opacity` 过渡，要么完全不透明要么不存在。透明度过渡会被拍到半透明，看上去像文字穿透提示框。
- **标题栏内的浮层必须绘制在正文之上**：`#titlebar` 需要显式 `z-index`（当前 `30`）。`#title-center` 带 `transform`，自身形成层叠上下文，其子元素的 `z-index` 越不出这个上下文，曾导致「在 Finder 中显示」提示被正文文字压住。
- 文件切换、覆盖或丢失未保存内容前，必须给出明确确认。
- 外部 Agent 修改文件时，使用已有的状态点和刷新机制，不新增常驻提示条。
- 导出功能统一放在 File 菜单中；当前支持 PDF 和 HTML。
- **菜单文案必须全部走 `labels`（中英同步），不自带图标**：能用系统 `role` 的地方才用（`undo`、`copy` 这类不画图标、文案由 `label` 覆盖），不要用会自己画图标、自己取英文名的 role。`recentDocuments` 就是反例：它在 macOS 上画出时钟图标并显示英文 “Open Recent”，而同一个菜单里其他项都是中文无声图标。

## 7. 实现约定

- 共享视觉样式放在 `src/renderer/themes/base.css`。
- 编辑器逻辑、IPC 逻辑和视觉样式分离。
- 主题颜色使用语义变量；新增主题变量时同步所有内置主题。
- 动态创建图标时使用固定的 SVG 模板，不拼接用户输入到 SVG 或 HTML。
- 新增控件前先检查是否已有相同职责的控件、样式或交互。
- 不为了单个控件复制一套 tooltip、按钮或图标样式。
- 改动 UI 后至少运行类型检查、构建和 `git diff --check`。

## 8. 贡献检查清单

提交 UI 或交互改动前，逐项确认：

- [ ] 这个元素确实必要，没有重复入口。
- [ ] 图标是线性 SVG，不是 Unicode 或 Emoji。
- [ ] 图标的尺寸、线宽、端点和连接与同类图标一致。
- [ ] 图标按钮有 `title`、`aria-label` 和 hover 文字说明。
- [ ] 颜色来自语义 CSS 变量。
- [ ] hover、active、disabled 状态完整且克制。
- [ ] 没有引入常驻工具栏、状态栏或额外面板（标签页只在用户主动创建后出现，且不持久化）。
- [ ] 菜单、快捷键和文件切换行为没有被破坏。
- [ ] 已完成类型检查、构建和差异检查。
- [ ] 如果规范不足，先补充本文档，再提交实现。
